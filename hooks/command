set -x

DENY_PATH=${BUILDKITE_PLUGIN_MACOS_SANDBOX_DENY_FILE_READ}
sudo -u ec2-user sandbox-exec -p \
    "(version 1) (deny file-read* (subpath \"${DENY_PATH}\")) (allow default)" \
    ${SHELL} -l -c -i "${BUILDKITE_COMMAND}"